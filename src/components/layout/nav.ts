/**
 * 사이트 내비게이션 단일 출처 — 헤더·모바일 메뉴·푸터가 함께 참조한다.
 * 컴포넌트가 아닌 순수 데이터라 서버/클라 어디서든 import.
 *
 * 항목을 둘로 줄였다(ADR-0025). 선택지가 많을수록 인지 부하가 커지고,
 * 이 사이트가 하는 일은 "글을 읽는 것"과 "누가 썼는지 아는 것" 둘뿐이다.
 *
 * 라벨은 영문 소문자다(ADR-0029). 이 사이트는 껍데기가 영문 활자로 돌아간다 —
 * 워드마크 CHANBIN., 히어로 PARK CHANBIN, 그리고 주소도 /posts·/about 이다.
 * 내비만 한글이면 거기서 문법이 한 번 끊긴다. 소문자인 것은 워드마크가 대문자 900이라
 * 내비까지 대문자면 둘이 서로 크기를 다투기 때문이다.
 */
export const NAV = [
  { href: "/posts", label: "posts" },
  { href: "/about", label: "about" },
] as const;
